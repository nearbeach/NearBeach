export default {
    methods: {
        showErrorModal: function(error,destination,location_id) {
            console.log("Error: ",error," | destination: ",destination," | location_id: ",location_id);
        }
    }
}